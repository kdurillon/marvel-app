{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <div class="card">
                    <img class="card-img-top" src="{{ character.thumbnail.StandardLarge }}" alt="{{ character.name }}">
                    <div class="card-block">
                        <h4 class="card-title">{{ character.name }}</h4>
                        <p class="card-text">{{ character.description }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}